# --- Authentication (GitHub OAuth) ---
# Create a new OAuth App on GitHub: https://github.com/settings/developers
GITHUB_ID=your_github_client_id
GITHUB_SECRET=your_github_client_secret

# --- NextAuth Configuration ---
# Generate a random string: openssl rand -base64 32
NEXTAUTH_SECRET=changeme_random_string_xyz
# In local: http://localhost:3000 | In Production: https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000

# --- Storage (Vercel Blob) ---
# Create a Blob store in Vercel and paste the Read/Write token here
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# --- Encryption (Fernet 32-byte Key) ---
# Run: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENVHUB_MASTER_KEY=your_generated_fernet_key

# --- Access Control (Critical) ---
# Comma-separated list of GitHub Organizations allowed to login (e.g. MyCompany,OpenAI)
ALLOWED_ORGS=YourOrgName
# (Optional) specific GitHub users allowed (e.g. for Admin/Personal use)
ALLOWED_USERS=your_github_handle

# --- Features ---
# Set to "true" ONLY if you want to show the specific demo sandbox
NEXT_PUBLIC_ENABLE_DEMO_MODE=false
